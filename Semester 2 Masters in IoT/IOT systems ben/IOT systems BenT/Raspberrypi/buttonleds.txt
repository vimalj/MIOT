#include <stdio.h>

#include<unistd.h>
#include<stdlib.h>
// passing parameters/arguements in main function
int main(int argc, char* argv[])  // (arg c arg count argv argument values)

/*
touch file.txt
sftp pi@10.9.32.131
sftp>

put file.txt
get file.txt

*/
//int loopctr = atoi(argv[1]);

//int main(void)
{
	int loopctr = atoi(argv[1]);


	FILE *fp = fopen( "/sys/class/leds/led0/trigger", "w+");
     	 fprintf (fp," none");
	fclose(fp);


	fp= fopen("/sys/class/leds/led0/brightness" ,"w+");


for (int i=0; i<5; i++)
{
	fprintf (fp, "1");
	fflush(fp);
	sleep(1);

	fprintf (fp, "0");
        fflush(fp);
        sleep(1);
}
fclose(fp);

	return 0;
}

