#include <stdio.h>
#include <stdlib.h>
 
int main() {
int c, n, r;

printf("Enter the value of N: \n");
scanf("%d", &r);
 
printf("N random numbers in [1,1000]\n");
 
  for (c = 1; c <= r; c++) {
    n = rand() % 1000 + 1;
    printf("%d\n", n);
  }
 
  return 0;
}